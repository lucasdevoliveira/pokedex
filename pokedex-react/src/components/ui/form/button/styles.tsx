import styled from "styled-components"

export const Button = styled.div`
  border-radius: 4px;
  border: 1px solid var(--color-red);
  padding: 10px 16px;
  color: var(--color-red);
  font-weight: 700;
  text-align: center;
`